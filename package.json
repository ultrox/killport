{
  "name": "@ma.vu/killport",
  "version": "1.2.12",
  "description": "CLI tool to kill processes on a port",
  "main": "dist/cli.js",
  "bin": {
    "killport": "dist/cli.js"
  },
  "scripts": {
    "build": "esbuild bin/cli.js --bundle --platform=node --minify --outfile=dist/cli.js",
    "test": "node --test",
    "prepublishOnly": "./scripts/prepublish.sh"
  },
  "devDependencies": {
    "esbuild": "0.27.2"
  },
  "keywords": [
    "cli",
    "port",
    "kill"
  ],
  "author": "Marko Vujanic",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/ultrox/killport.git"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
